import React from 'react'
export const metadata = {
  title: `A.G.A Ants' Golden Age - Home`,
  description: 'Home',
}

const layout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>
}
export default layout
